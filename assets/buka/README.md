# 布卡形象使用规范

- 主形象统一使用 `buka-main.svg`。
- 眨眼动画统一叠加 `buka-main-blink.svg`。
- 成功、完成、鼓励场景可使用 `buka-main-happy.svg`。
- 诊断、分析、等待结果场景可使用 `buka-main-thinking.svg`。
- 不再使用脸颊红晕版本，避免整体气质偏幼。
- 新页面优先复用这一套蓝色水滴形象，不要混用其他布卡外形。
