<view class="bind-page">
  <view class="card bind-hero">
    <text class="bind-title">已购课学员先绑定一下账号</text>
    <text class="bind-desc">绑定成功后，聊天消息会跟你的正式学习档案关联起来，后续就不用重复操作了。</text>
  </view>

  <view class="card bind-form">
    <text class="bind-label">购课手机号</text>
    <input
      class="bind-input"
      type="number"
      maxlength="11"
      value="{{phone}}"
      placeholder="请输入报名时使用的手机号"
      bindinput="onPhoneInput"
      confirm-type="done"
    />
    <button class="btn-primary bind-submit" loading="{{submitting}}" bindtap="submitBind">
      绑定并继续发送
    </button>
    <text class="bind-hint">如果不是已购课学员，也可以先返回继续浏览咨询内容。</text>
  </view>
</view>
